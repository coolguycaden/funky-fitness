import 'package:flutter/material.dart';

class EditExerciseBox extends StatelessWidget {
  const EditExerciseBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}